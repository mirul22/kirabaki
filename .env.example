# Local Postgres — see docker-compose.yml
DATABASE_URL=postgres://kirabaki:kirabaki@localhost:5432/kirabaki

# Better Auth — generate with: openssl rand -base64 32 (at least 32 characters)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
