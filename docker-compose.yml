services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kirabaki
      POSTGRES_PASSWORD: kirabaki
      POSTGRES_DB: kirabaki
    ports:
      - "5432:5432"
    volumes:
      - kirabaki_pg:/var/lib/postgresql/data

volumes:
  kirabaki_pg:
